# 导入合计剧本，没有合理的分集的页面设计
# 现在导入剧本后在资产页上面，也不方面，建剧本可以放在单独页面里。我们不是说过可以有分集梗概吗？可以做这些预处理。当时是怎么设计的
# 现在点击提取资产没有反应，资产卡片内，图片显示不全，建议可以增加列表、宫格、展示方式的选择。
# 集数展示的位置不太合理，而且增加集数点击后，也没有合理的UI显示新增集数和导入剧本的UI。这些前端功能都有，但显示不合理
